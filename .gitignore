# npm artifacts from local schema validation runs
node_modules/
package-lock.json
package.json

# ajv cache
.ajv-cache/

# Rust workspace build artifacts (root + per-crate targets)
target/
validator/target/
scanner/target/

# Claude Code per-session state and local settings
.claude/memory/tmp/
.claude/settings.local.json
